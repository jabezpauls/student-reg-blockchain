{
  "name": "student-reg-blockchain",
  "version": "1.0.0",
  "description": "A decentralized application for storing student information on the Ethereum blockchain",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "hardhat compile && node scripts/copy-abi.js",
    "setup": "npm run compile && hardhat run scripts/setup-ganache.js --network localhost",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "dev": "cd frontend && npm run dev",
    "ganache": "npx ganache --port 8545 --host 127.0.0.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jabezpauls/student-reg-blockchain.git"
  },
  "keywords": [
    "blockchain",
    "ethereum",
    "solidity",
    "react",
    "dapp",
    "web3",
    "metamask",
    "hardhat"
  ],
  "author": "jabezpauls",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.26.3"
  }
}
